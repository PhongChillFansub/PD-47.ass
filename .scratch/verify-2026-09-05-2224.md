# Verify report — output của session `arena/01a06dcc-pd-47-ass`

> Chạy theo checklist trong `.scratch/handoff-2026-09-05-2157.md` §3.
> Session verify: `arena/01a0739a-pd-47-ass`, 2026-09-05 22:24 UTC.
> **Đã tuân thủ quy tắc của handoff: KHÔNG tự sửa bất kỳ artifact nào.** File này là báo cáo mới duy nhất được thêm.
> Baseline verify: HEAD = `7ad2074` (merge PR #26) = tree của `99c5fc8`.

---

## 0. TL;DR

Nghi ngờ của chủ repo là **có cơ sở**, nhưng sai sót nằm chủ yếu ở **số liệu + trích dẫn + một quyết định bị bỏ sót**, không phải ở nội dung kiến trúc.

- **3 lỗi kỹ thuật thật** mà cả spec lẫn code review đều bỏ sót (mục A) — 1 cái có khả năng chặn extension load.
- **11 chỗ số liệu sai/lệch** giữa các artifact (mục B).
- **1 mâu thuẫn ADR ↔ spec** về trạng thái quyết định (mục D1) + 1 trích dẫn Q đáng ngờ (D2).
- **Spec vi phạm rule của template `/to-spec`** ("không đưa file path / code snippet") — và đúng như template cảnh báo, path đã outdated ngay (mục E1 + A2).
- **Handoff file tự nó có 7 mệnh đề đã sai** tại thời điểm verify (mục G) — quan trọng nhất: nói "chưa commit/chưa push/label chưa apply", trong khi thực tế đã commit `99c5fc8`, đã merge PR #26, và label `ready-for-agent` **đã** apply.
- **Cập nhật 05sep26 ~23:00 UTC**: chủ repo đã trả lời 3 điểm nhóm 1 và chỉ thị sửa nhóm 1+2+3+4. Đã sửa xong 6 file tài liệu (xem mục **L**); issue #25 không sửa được vì bot thiếu quyền (L7); còn **2 điểm chờ quyết**: D1 (retry backoff) và D2 (trích dẫn Q54) — xem L8.
- Phần **PASS**: cấu trúc spec (19 stories, 21 OOS, 7 open questions — **nay còn 6** sau khi D1 được chốt 05sep26, 11 term phụ lục B), 7 ADR đủ số + nội dung khớp mô tả, ADR 0007 khớp code thật từng chi tiết, không có sensitive info, code review đúng 7 hard + 12 mùi, mô tả `popup.html` trong spec khớp 100%.

---

## A. Lỗi kỹ thuật thật (spec + code review cùng bỏ sót) — **cần chỉ thị**

### A1. `manifest.json` trỏ tới `options.html` KHÔNG TỒN TẠI → có khả năng extension không load được

```
manifest.json:  "options_page": "options.html"
find . -name "options*"  →  (0 kết quả)
```

Chrome validate path `options_page` lúc load unpacked; file thiếu → lỗi load. Trong khi đó:

- Spec §1.3 xếp hạng `manifest.json` = **"ổn"**.
- Code review §A liệt kê 7 hard violation, **không có mục nào** nói tới việc manifest trỏ tới file không tồn tại (§A.4 chỉ nói "chưa có options page" như một tính năng thiếu, §A.7 chỉ bàn `host_permissions`).

Sandbox không có Chromium nên chưa test load thật được → **cần chủ repo xác nhận trên máy thật**. Đây là ứng viên "hard violation #8" mà code review bỏ sót.

### A2. Module map §2.2.1 mâu thuẫn với `manifest.json` + `popup.html`, nhưng §4.10 không liệt kê chỗ phải sửa

| Spec nói | Repo thật | Hệ quả |
|---|---|---|
| §2.2.1 `popup/popup.html` + `popup/popup.js` ("đã có, giữ nguyên") | `popup.html` + `popup.js` ở **root**; `manifest.json` có `"default_popup": "popup.html"`; `popup.html:42` có `<script type="module" src="popup.js">` | Dời vào `popup/` mà không sửa manifest → popup gãy |
| §2.2.1 + §4.15 `background/options.html` + `background/options.js` | `manifest.json` có `"options_page": "options.html"` (root) | Implement theo spec → manifest trỏ sai chỗ |
| §4.10 "Manifest / permissions" | Chỉ bàn `permissions` + `host_permissions` + CSP | **Không hề nhắc** phải đổi `default_popup` / `options_page` |

Người implement đọc §4.10 sẽ "giữ nguyên" manifest và tạo ra extension gãy. Đây chính là hệ quả mà template `/to-spec` cảnh báo khi cho phép đưa file path vào spec (xem E1).

### A3. Code review khẳng định sai về auto-map `#<videoId>` (Story #4)

Code review, trục Spec, bảng §A:

> `§3.1 Story 4: Auto-map #<videoId> trong tên file` | `fetcher.js:805+` (**chưa đọc chi tiết nhưng đã có regex theo pipeline**) | Cần verify code

Kết quả verify:

```
grep -n "videoId" background/fetcher.js            → 0 kết quả
grep -nE "\{11\}|A-Za-z0-9_-|watch\?v=" fetcher.js  → 0 kết quả
grep -n "videoId\|#<" pipeline.txt                  → chỉ có ASSCEE_subData_<videoId> (storage), KHÔNG có quy ước tên file
```

Ký tự `#` trong `fetcher.js` là **prefix case-sensitive của search token** (`parseSearchQuery`, dòng 94–118: `const caseSensitive = raw.charAt(0) === '#'`), hoàn toàn khác với tag `#<videoId>` trong tên file. Nhiều khả năng reviewer đã nhầm 2 khái niệm này.

→ **Story #4 + §4.3.1 là tính năng CHƯA implement ở bất kỳ đâu**, không phải "đã có regex, cần verify". Dòng này nên là ❌ Thiếu. Đây là đúng loại lỗi "hallucinate do context lớn" mà chủ repo nghi ngờ.

---

## B. Số liệu sai / lệch giữa các artifact

| # | Claim | Thực tế verify | Nằm ở đâu |
|---|---|---|---|
| B1 | "**90/90 pass**", gán cho `tests/parser.test.mjs` | `npm test` hôm nay: **91 pass / 0 fail** (parser 39, storage 28, tagProcess 22, fetcher 2). Riêng `parser.test.mjs` = **39** test, không phải 90. Số 90 đến từ `pipeline.txt:339,521` = **toàn suite** tại 03sep26 | spec §1.3, §5.1.1, §5.3; TOC §5.2; body issue #25; code review phụ lục |
| B2 | CONTEXT.md "**32 term**" (Actors 3 + ASS/Aegisub **8** + Architecture 11 + Renderer **10**) | **35 term**: Actors 3 + Sub files & Aegisub **9** + Extension architecture 11 + Renderer internals **12** | handoff §1, §2, §3.5, §3.8 |
| B3 | Spec "**~700 dòng**" | **842 dòng** | handoff §1, §2 |
| B4 | "**64 câu Q&A**, 16 round" | TOC appendix liệt kê range `Q1–Q47` rồi `Q49–Q64` → **thiếu Q48**, đếm được **63** câu. Thêm: R11 ghi `Q39–Q43` (5 số) nhưng chỉ tóm tắt 4 ý | TOC §0 + appendix; spec §0 (dòng 4); body issue #25; handoff |
| B5 | TOC §1: `storage.js` "11 export, **11 key**" | 11 export ✓ nhưng **5 key** (`ASSCEE_sourceList`, `ASSCEE_subData`, `ASSCEE_subIndex`, `ASSCEE_config`, `ASSCEE_renderData`). Spec §1.3 đã sửa đúng ("11 export + 5 key"), TOC thì không | TOC §1 |
| B6 | TOC §4.1: `tests/renderer.test.mjs` "**low-level**" | TOC §5.1 + spec §5.1.2 xếp nó là "**mid level**" → TOC tự mâu thuẫn | TOC §4.1 vs §5.1 |
| B7 | Code review header: "Diff = **99 files, 8301 insertions**" | Đó là cây **baseline `ab3b264`** (verify qua GitHub API: 99 blob). Branch tip tại merge = **111 files / 10104 insertions**. 12 artifact mới = 1804 dòng; 10104 − 1804 = 8300 ≈ 8301 ✓ → header dán nhãn fixed point sai (nói "branch … vs empty tree") | code review header |
| B8 | Code review §D: "Judgement calls **~5**" | Đếm trong §B: B.2(1) + B.4(3) + B.5(2) + B.7(1) + B.8(1) + B.11(1) ≈ **9 item** rải trong 6/12 mùi; 6 mùi còn lại ghi "không có finding đáng kể" | code review §B/§D |
| B9 | Code review §A.1: "`background.js` chỉ **2 dòng**" | Cùng mục liệt kê **3** dòng, phụ lục ghi "**3 dòng**". Thực tế: 3 dòng nội dung (`wc -l` = 2 vì thiếu newline cuối) | code review §A.1 vs phụ lục |
| B10 | Code review §A.2: "`popup/popup.js` rỗng" | Path hiện tại là **`popup.js` ở root**; `popup/` chỉ là đề xuất trong module map | code review §A.2 |
| B11 | Line-number drift nhỏ | `SUBTITLE_DATA_KEY_BASE` ghi line 22 → thực **26**; `crypto.randomUUID()` ghi 99 → thực **97**; `storage.js` ghi 366 dòng → thực **365**; `fetcher.js` ghi 864 → thực **863** | code review §B.1, §B.9, §B.8, phụ lục |

Các line-ref còn lại của code review đều **đúng chính xác**: `enqueueWrite` 49, `validateSourceUrl` regex 63–72, `addSource` 82, `utils.log` trong addSource ~102, `addSubData` 152 (+ comment `to-do` 144), `buildCacheEntry` 215, `useSubData` 232, `getConfig` 285, `setRendererStat` 343, `RENDERER_STAT_COOLDOWN_MS = 500` ở 36, `searchSubtitleFile` 392, `fetchSubtitleFileList` 805, `classifyDecoration` 340 (TODO 341), `classifyCollision` 355, `classifyClip` 382 (TODO 383), `utils.js` logger/alias 17–34, `parser.js` 898 dòng, `popup.html` 43 dòng.

---

## C. Terminology / glossary (chuẩn cứ: `docs/agents/domain.md` + `CONTEXT-FORMAT.md`)

- **C1 — Spec drift sang từ bị glossary cấm.** `CONTEXT.md` chốt `sub provider`, `_Avoid_: fansub author`. Nhưng spec dùng "fansub author" ở **§3.2 heading** và **Phụ lục B dòng 841**; TOC story #13 cũng vậy. `domain.md`: "Don't drift to synonyms the glossary explicitly avoids."
- **C2 — Spec dùng "user" cho viewer** ở §4.14 ("nút 'Mới nhất' hiện khi **user** scroll lên") và §7.3 ("Nếu **user** có nhiều cache"), trong khi `_Avoid_: user`. (Các chỗ "user story" / "user-facing" là thuật ngữ của template → OK.)
- **C3 — Spec dùng "segment"** ở §4.2.1 (comment `// text segment`), §4.5.3 ("multi-segment `\move`"), §7.2 ("đo segment một lần"), trong khi glossary chốt `base` với `_Avoid_: segment (đã đổi tên ở 02sep26)`. Có thể biện minh nếu "segment" ở đây là khái niệm của pretext/`\move` chứ không phải `lineCss[i].base[j]` — nhưng glossary chưa tách 2 nghĩa → **cần chủ repo chốt**.
- **C4 — CONTEXT.md thiếu 3 term mà chính spec định nghĩa ở Phụ lục B**: `libass` (0 lần trong CONTEXT.md, 4 lần trong spec), `MV3` (chỉ xuất hiện như tính từ trong định nghĩa khác, không có term riêng), `PlayerAdapter` (**0 lần trong CONTEXT.md, 10 lần trong spec**, là interface trung tâm của §4.16). Theo `domain.md`, đây là "real gap" cần ghi nhận.
- **C5 — CONTEXT.md chứa implementation detail**, trái rule `CONTEXT-FORMAT.md` ("*totally devoid of implementation details*", "*One or two sentences max*"):
  - `sub slot` → storage key `ASSCEE_subData_<videoId>` + hành vi overwrite (thuộc ADR 0005)
  - `storageId` → `crypto.randomUUID()`
  - `message bus` → `chrome.runtime.sendMessage` / `onMessage`
  - `FileEntry`, `parsedData`, `styleCss`, `lineCss`, `base` → liệt kê field/struct
  - `FALLBACK_DEFAULT_STYLE` → "Định nghĩa trong `background/parser.js`" (file path)
  - `skip frame` → ngưỡng "dropped ratio > 30% trong 5 giây" (thuộc ADR 0006)
  - `NPS` → biến `nodesTouched`
  - `content script` → "không fetch/parse/storage" (quyết định kiến trúc, thuộc ADR 0001)
- **C6 — `FileEntry` là term duy nhất trong 35 term không có dòng `_Avoid_`** (34/35 có) → fail checklist §3.5 "Mỗi term có _Avoid_".
- **C7 — 2 storage key có thật trong code nhưng không có term glossary**: `ASSCEE_subIndex` (`storage.js:30`) và `ASSCEE_renderData` (`storage.js:34`) — cả hai đều có trong spec §4.9.1. (Handoff §3.1 gọi `subData`/`subIndex`/`renderData` là "term trong glossary" → **không đúng**, chỉ `ASSCEE_subData_<videoId>` xuất hiện lồng bên trong định nghĩa `sub slot`.)

---

## D. ADR ↔ spec ↔ code

- **D1 — Mâu thuẫn trạng thái — ✅ ĐÃ GIẢI QUYẾT 05sep26.** Chủ repo xác nhận: *"ko có Retry với exponential backoff"* → ADR 0003 đúng, spec sai trạng thái. Đã sửa: ADR 0003 thêm `Status: accepted`; spec §4.9.6 đổi "CHƯA CHỐT" → "**ĐÃ CHỐT — KHÔNG làm**"; OOS #17 đổi "chưa chốt" → "đã chốt KHÔNG làm"; §7.1 rút mục #3 (còn **6** open question, đánh số lại) + thêm ghi chú; §8 đổi "Chốt 7" → "Chốt 6"; TOC §4.9 + §7 mục 3 thêm chú thích đã chốt. *(Nguyên văn phát hiện:)* ADR 0003 ghi như một quyết định **đã chốt**: "*KHÔNG có retry với exponential backoff*". Nhưng spec vẫn để mở ở **3 chỗ**: §7.1 open question #3 ("có cần không?"), §4.9.6 ("**CHƯA CHỐT**"), OOS #17 ("chưa chốt, có thể không cần"). → Hoặc ADR 0003 vượt quá mức đã chốt trong grilling, hoặc spec phải bỏ open question #3. `domain.md` yêu cầu flag tường minh thay vì âm thầm override.
- **D2 — Trích dẫn Q đáng ngờ — ✅ ĐÃ GIẢI QUYẾT 05sep26.** Chủ repo xác nhận mệnh đề và cho lý do: *"CS ko cần source data vì CS ở đây là content script/content-side (phần script chạy ở web page, ko phải background-side)"*. Cách xử lý: **ghi thẳng lý do đó vào ADR 0001 + spec §4.5.1** thay vì dựa vào số câu không kiểm chứng được (kèm chú thích vì sao bỏ trích Q54: phụ lục TOC mô tả Q54 = "BG chỉ fetch on options page action", thuộc OOS #13). `CONTEXT.md` term `content script` cũng được sharpen thành "phía **content-side** … đối lập với background-side". TOC §4.7 thêm chú thích đính chính. *(Nguyên văn phát hiện:)* ADR 0001 và spec §4.5.1 cùng trích "**Q54**" cho mệnh đề "*CS không bao giờ cần source data*". Theo TOC appendix R14 (`Q53–Q56`), Q54 = "*BG chỉ fetch on options page action*" — và spec OOS #13 dùng Q54 **đúng** với nghĩa đó. Mệnh đề "CS thuần renderer" khớp **Q23** (R6: "BG xử lý phần lớn, CS render") hơn. Không kết luận dứt điểm được vì grilling transcript không persist → **cần chủ repo xác nhận**.
- **D3 — Format ADR lệch `ADR-FORMAT.md`.** Template của skill = `# {Short title}` + **1–3 câu**. Cả 7 ADR dùng 3 section tự đặt tên (**Bối cảnh** / **Quyết định** / **Tại sao**), dài ~150–250 từ, và H1 có prefix số (`# 0001 — …`) trong khi format để số ở tên file. Nội dung tốt (đủ rejected alternatives = "Considered Options" hợp lệ), nhưng **handoff §3.6 tự mô tả "tiêu đề ngắn + 1-3 câu tóm tắt" là không khớp với artifact thật** → checklist đặt sai tiêu chí.
- **D4 — Không có ADR nào mâu thuẫn nhau.** Đối chiếu cả 7: 0001 ↔ 0004 ↔ 0006 nhất quán về vai trò CS (renderer, kể cả canvas vẫn là renderer); 0003 ↔ 0005 nhất quán về cache/manual; 0002 ↔ 0007 không giao nhau; không ADR nào nói "CS fetch".
- **D5 — ADR 0007 verify khớp code thật 100%.** `parser.js:389` → `'-webkit-text-stroke-width': style.outline ? \`${style.outline * 2}px\` : '0px'`, JSDoc giải thích ở 287–293, comment nội bộ 387–388, box `borderStyle==3` reset stroke 0px ở 382–385. Trích dẫn "`pipeline.txt` 02sep26 §5" **chính xác**: pipeline.txt dòng 410–421 (mục "5. Baseline Chromium 131+") chứa "ÁP DỤNG stroke ×2 (chốt)".
- **D6 — Không ADR nào "thừa"** theo 3 tiêu chí của skill. Ứng viên yếu nhất là **0006** (đổi threshold 30%/5s khá rẻ → tính "hard to reverse" yếu), nhưng vẫn đạt tiêu chí 2 (surprising) + 3 (real trade-off: best-effort thuần vs preset cố định vs dynamic). 6 ADR còn lại đạt cả 3.

---

## E. Spec vs template `/to-spec`

- **E1 — Vi phạm rule tường minh của template.** `<spec-template>` mục Implementation Decisions: "*Do NOT include specific file paths or code snippets. They may end up being outdated very quickly.*" Spec chứa rất nhiều: §2.2.1 (nguyên cây thư mục + path), §4.2.1–§4.2.4 (code block shape), §4.5.4 (pseudo-code rVFC loop), §4.9.1 (tên storage key), §4.10 (chuỗi permission), §4.15 (path options), §4.16 (`interface PlayerAdapter {}` + `document.querySelector(...)`). Ngoại lệ của template chỉ dành cho snippet **sinh ra từ prototype** — ở đây không có prototype. Và lời cảnh báo đã ứng nghiệm ngay: xem **A2**.
- **E2 — "8 phần theo template" là mô tả sai template.** Template có **7** section (Problem Statement, Solution, User Stories, Implementation Decisions, Testing Decisions, Out of Scope, Further Notes). §8 "Workflow tiếp theo" + Phụ lục A/B là phần spec tự thêm (không sai, nhưng handoff §3.2 nói "8 phần đầy đủ … theo template `/to-spec`" là không chính xác).
- **E3 — Spec không nói cách chạy test.** Không có `npm test` / `node --test` / `package.json` ở bất kỳ đâu trong spec lẫn TOC, dù §5 Testing Decisions khá chi tiết và module map §2.2.1 liệt kê gần hết repo.
- **E4 — Mâu thuẫn nội bộ spec về test file.** §5.1.2 khai `tests/background.test.mjs` (mới) nhưng module map §2.2.1 chỉ có 5 file test (parser, storage, fetcher, tagProcess, renderer) → thiếu `background.test.mjs`.
- **E5 — Mâu thuẫn tên field `title` vs `subTitle`.** §4.2.2 (`renderer/stat` payload `{ fps, nps, dfps, title }`) và §4.9.1 ("fps, nps, dfps, title") dùng **`title`**; §4.14 ("đọc từ `ASSCEE_renderData.subTitle`") và **code thật** (`storage.js` JSDoc dòng 327, 338, 341) dùng **`subTitle`**. §4.9.1 còn ghi "giữ nguyên hiện tại" → sai tên field so với hiện tại. (Code review §B.1/§B.4 dùng `subTitle` đúng.)
- **E6 — Phụ lục A thiếu tham chiếu.** Hàng "§6 OOS" liệt kê Q25b, Q30, Q34, Q35, Q49, Q54, Q55, Q59, Q60 nhưng §6 thực tế còn trích **Q20** (OOS #12) và **Q22** (OOS #1). Phụ lục A cũng không có hàng cho **§4.14 Popup** (story #10) và **§4.15 Options page** (story #9), trong khi lại gán story #10 cho "§4.5 Renderer".
- **E7 — `Q25b` không truy vết được.** Spec §6 OOS #4 trích "Q25b", nhưng enumeration của TOC chỉ có Q24–Q27, không có ID dạng chữ-số-hậu-tố (vd `Q25b`) → không đối chiếu được với phụ lục.
- **E8 — Header spec stale ngay trong cùng commit.** Dòng 6: "*vì `CONTEXT.md` chưa được tạo*" — nhưng commit `99c5fc8` thêm **cả** `CONTEXT.md` **và** file spec này. TOC dòng 13 ("cho đến khi `CONTEXT.md` được tạo") cùng vấn đề (TOC là phụ lục lịch sử nên có thể chấp nhận, spec thì không).

---

## F. Repo hygiene / doc cũ chưa được cập nhật

- **F1** `AGENTS.md:17` vẫn ghi "*một `CONTEXT.md` + `docs/adr/` ở repo root (tạo lười khi cần, **chưa có sẵn**)***" → nay đã có cả hai.
- **F2** `docs/agents/domain.md:40` vẫn ghi "*Treat it as a primary source … **until `CONTEXT.md` exists***" → nay đã tồn tại.
- **F3** Module map §2.2.1 không liệt kê `CONTEXT.md`, `docs/adr/` (7 file) và `docs/specs/v1-pd47ass.codereview.md` — đều là artifact của chính session đó.
- **F4** **Artifact không nằm trên `main`.** `origin/main` chỉ có `LICENSE` + `README.md` (Initial commit `38b24fe`). PR #26 merge vào **`editor`** (baseRefName = `editor`). Issue #25 link tới blob của branch `arena/01a06dcc-pd-47-ass` — branch **vẫn còn** trên origin nên 4 link hiện đang sống (đã verify qua API), nhưng sẽ **gãy toàn bộ** nếu branch bị xóa sau merge. Nên trỏ về `editor` hoặc SHA `99c5fc8`.
- **F5** *(pre-existing, không phải lỗi session trước)* `AGENTS.md` ghi "Nguồn cài: commit `3cca18b` (**05sep26**)": `3cca18b` có thật trong `mattpocock/skills` nhưng dated **2026-09-04**, và commit cài trong repo này (`3bd4537`) cũng dated 04sep26 → ngày trong AGENTS.md lệch 1 ngày.

---

## G. Chính file handoff có 7 mệnh đề đã sai

| # | Handoff nói | Thực tế verify |
|---|---|---|
| G1 | §8 + header: "Tất cả file mới đều **UNTRACKED** (chưa commit)", "Session trước **KHÔNG push** branch" | Đã commit `99c5fc8` lúc **21:58:54Z** (~90 giây **sau** timestamp handoff 21:57); branch `arena/01a06dcc-pd-47-ass` **có trên origin**; **PR #26 đã MERGED** vào `editor` lúc 22:00:22Z; `git status` sạch; `.scratch/handoff-…md` **đang được track**. **Đính chính 05sep26 — KHÔNG phải lỗi của session trước**: chủ repo xác nhận handoff được viết *trước khi* chính chủ repo merge và sửa label, nên 3 mệnh đề này đúng tại thời điểm viết, chỉ lỗi thời sau đó. Đã đánh dấu ~~gạch ngang~~ + ghi chú trong handoff. |
| G2 | §3.9 + §8: "Label `ready-for-agent` **CHƯA APPLY** (do bot permission), nhắc chủ repo apply thủ công" | Label **ĐÃ apply** trên issue #25 (`gh issue view 25` trả về label `ready-for-agent`, id `LA_kwDOT5zgn88AAAACz-SqPQ`). **Đính chính 05sep26**: chính **chủ repo apply thủ công** sau khi handoff được viết → mệnh đề gốc đúng lúc viết. Suy ra: bot token của Arena **không có quyền ghi issue** trong repo này (xem L5). |
| G3 | §2/§3.5/§3.8: "spec 700 dòng", "CONTEXT.md 32 term", "nhóm 8 + 10", §7: "tests/ 90/90 pass" | 842 dòng; 35 term; 9 + 12; 91/91 (xem B1, B2, B3) |
| G4 | §6 các lệnh shell | **cmd5 hỏng**: `grep -c "^- \*\*[^*]"` để đếm OOS trả **84** (đếm cả file), không phải 21 — vì OOS là list đánh số `1. **…**`. **cmd8 vô dụng**: grep slug ADR trong spec → 0/7 match. **cmd6 false-negative 5/29 term** vì handoff viết gạch nối (`sub-provider`, `content-script`, `sub-slot`, `chuẩn-bổ-sung`, `sub-DOM`) còn doc viết cách. cmd2 ✓ 7, cmd3 ✓ 35 (mâu thuẫn với prose "32 term" của chính handoff), cmd4 ✓ 19 |
| G5 | §1/§8: "Skill `/handoff` khuyến nghị lưu ở `/tmp/`" | **Đúng** — `SKILL.md`: "*Save to the temporary directory of the user's OS - not the current workspace.*" Nhưng hệ quả chưa được nêu: vì file nằm trong workspace nên nó **đã được commit vào git** và giờ tồn tại vĩnh viễn trong lịch sử `editor` |
| G6 | §3.1: grep `subData`/`subIndex`/`renderData` như "term code-internal **trong glossary**" | Chỉ `FALLBACK_DEFAULT_STYLE` là term thật. 3 cái còn lại là **storage key trong code** (`storage.js:26,30,34`), không phải term của CONTEXT.md → xem C7 |
| G7 | §2: "Repo không thay đổi code — chỉ thêm file mới", "Code hiện tại vẫn ở `ab3b264`" | **ĐÚNG** ✓ — `compare ab3b264...99c5fc8`: 12 file, toàn bộ là addition, 0 modification; `ab3b264` = merge PR #24 (2026-09-04) |

---

## H. Checklist §3 — kết quả từng mục

### 3.1 Cross-reference
- [x] CONTEXT.md vs code — **PARTIAL**: `FALLBACK_DEFAULT_STYLE` ✓ (`parser.js:66`), `ASSCEE_subData/subIndex/renderData` ✓ (`storage.js:26/30/34`). Nhưng `subIndex` + `renderData` **không có term glossary** (C7), và CONTEXT.md thiếu `libass`/`MV3`/`PlayerAdapter` (C4).
- [x] CONTEXT.md vs spec — **FAIL**: C1 (fansub author), C2 (user), C3 (segment). Không có "end-user" trong spec ✓.
- [x] 7 ADR mâu thuẫn nhau — **PASS** (D4). Không ADR nào nói "CS fetch".
- [x] Spec vs 7 ADR — **FAIL 1 chỗ**: D1 (retry backoff: ADR chốt, spec để mở). Còn lại khớp (cooldown 60s ✓ §4.9.3; DOM+canvas ✓ §4.5.3; slot overwrite ✓ §4.8.3; skip dynamic+detect ✓ §4.5.5; stroke ×2 ✓ §4.6 + code).
- [x] Code review vs spec — **FAIL**: review nêu finding mà spec chưa cover (`getConfig` return type, `useSubData` thiếu shape check) → nhất quán với open question; nhưng review **bỏ sót** A1, A2, A3 và hedge sai ở Story #4.

### 3.2 Spec `v1-pd47ass.md`
- [x] 8 phần + Phụ lục A/B — **PASS** (nhưng template chỉ có 7 → E2)
- [x] 19 user stories, đánh số 1–19 liên tục, không trùng/thiếu — **PASS**
- [x] 21 OOS, đánh số 1–21 liên tục — **PASS**
- [x] §7.1 có 7 open question — **PASS** lúc verify; **nay còn 6** (mục #3 retry backoff được chủ repo chốt 05sep26 là KHÔNG làm → rút khỏi open question, spec đánh số lại 1–6 và §8 đổi "Chốt 7" → "Chốt 6")
- [x] Số liệu §2.2.1 — **FAIL**: handoff mong "6 module background + 4 test + 1 HTML + 1 manifest + 1 package.json"; module map thực tế có **8** file trong `background/` (6 `.js` + `options.html` + `options.js`), **5** file test, **2** file trong `popup/`, **9** trong `content/`, và **không có `package.json`** ở bất kỳ đâu trong spec (E3). Bộ số của handoff khớp §1.3 hơn, trừ `package.json` (cũng vắng).
- [x] Phụ lục A trỏ đúng § — **PARTIAL** (E6)
- [x] Phụ lục B 11 term — **PASS** về số lượng (Aegisub, ASS, libass, MV3, rVFC, Karaoke, pretext, chuẩn bổ sung, PlayerAdapter, Sub provider, Sub slot); trùng CONTEXT.md 8/11, định nghĩa nhất quán **trừ** "fansub author" (C1)

### 3.3 TOC `v1-pd47ass.toc.md`
- [x] §8 dùng `docs/specs/v1-pd47ass.md` — **PASS**
- [x] §4.3 tham chiếu `docs/specs/v1-pd47ass` — **PASS** (lưu ý: TOC là outline viết **trước** spec mà lại trích spec làm chuẩn cứ → vòng tròn thời gian, nên thêm ghi chú)
- [x] §6 có ghi chú Q33 — **PASS**, và khớp TOC R9 (Q33 = "không cần OOS") ✓
- [x] 16 round × ~4 câu = ~64 — **FAIL**: thiếu Q48, đếm được 63 (B4)
- [x] TOC giữ làm phụ lục — **PASS**
- [x] *Phát hiện thêm*: R12 vẫn ghi "file ở `docs/specs/v1-renderer.md`" mà **không có chú thích đổi tên** → người đọc dễ tưởng file đó tồn tại; §1 sai "11 key" (B5); §4.1 vs §5.1 mâu thuẫn level của `renderer.test.mjs` (B6)

### 3.4 Code review
- [x] 2 trục rõ ràng — **PARTIAL**: heading `## Standards` / `## Spec` tách đúng, không merge finding ✓. Nhưng mục "Tổng kết 2 trục" kết luận "*Cả 2 trục đều fail … vì cùng 1 lý do*" và đưa **1 danh sách hành động ưu tiên gộp 1–7** → vi phạm tinh thần skill ("*Don't pick a single winner across axes: that's the reranking the separation exists to prevent*"). Ngoài ra skill giới hạn **400 từ/trục**; báo cáo này ~1800 từ (Standards) + ~900 từ (Spec).
- [x] §A có 7 hard violation, đúng danh sách handoff mô tả — **PASS** (nhưng thiếu A1; nội dung A.2 sai path — B10)
- [x] §B có 12 mùi (B.1–B.12) — **PASS**
- [x] Tổng kết §D: 7 hard + ~5 judgement; ~14/21 thiếu — **PARTIAL**: "~5 judgement" đếm thiếu (B8); "~14/21 thiếu" hợp lý; "**~5/21 đã implement đúng**" đếm được 6 story ✅ (1, 2, 5, 14, 18, 19) + 1 "nửa" (10)
- [x] Worst issue mỗi trục khớp §A.1 + §D — **PASS** về mặt văn bản; lưu ý 2 "worst issue" thực chất là **cùng một vấn đề** (`background.js` rỗng = BG message bus thiếu)

### 3.5 `CONTEXT.md`
- [x] Header + mô tả — **PASS** (`# PD-47.ass — Domain Context` + 2 câu; có thêm 1 đoạn meta không bắt buộc)
- [x] 4 nhóm = 32 term — **FAIL**: 4 nhóm ✓ nhưng **35 term** (3 + 9 + 11 + 12)
- [x] Mỗi term có `_Avoid_` — **FAIL**: 34/35, thiếu ở `FileEntry` (C6)
- [x] Không chứa implementation detail — **FAIL**: 8 term vi phạm (C5)
- [x] Không chứa general programming concept — **PASS về cơ bản** (không có Promise/regex/queue). Biên: `message bus` và `FPS` là khái niệm khá chung, nhưng cả hai đều được định nghĩa đặc thù cho repo → chấp nhận được

### 3.6 7 ADR
- [x] Đủ 7 file, đánh số 0001–0007 liên tục — **PASS**
- [x] Nội dung từng ADR khớp mô tả của handoff — **PASS cả 7** (0001 có "tại sao" ở cuối ✓; 0002 có 2 phần + lý do giới hạn 8 key ✓; 0003 so sánh retry backoff ✓; 0004 so sánh 3 hướng ✓; 0005 so sánh ETag/Update Details ✓; 0006 có 2 cơ chế + ngưỡng 30%/5s ✓; 0007 giải thích magic number 2 ✓)
- [x] Đúng format skill — **FAIL về format** (D3: 3 section tự đặt, dài hơn "1–3 câu", H1 có prefix số)
- [x] Đủ 3 tiêu chí, không ADR thừa — **PASS** (D6; 0006 yếu nhất ở tiêu chí "hard to reverse")
- [x] ADR 0007 khớp code thật — **PASS** (D5, verify tới số dòng)

### 3.7 Sensitive info
- [x] Không API key / password / token — **PASS** (các hit "token" là search token; "bot token permission" là mô tả)
- [x] Không email / PII — **PASS** (0 kết quả regex email trên toàn bộ artifact + `pipeline.txt` + `manifest.json`)
- [x] URL là org/repo public — **PASS** (`github.com/PhongChillFansub/PD-47.ass`, `github.com/chenglou/pretext`, youtube/drive/github/bilibili wildcard)
- [x] Issue #25 public — **PASS**

### 3.8 Nhất quán nội bộ
- [x] "16 round" — **PASS** (TOC §0, TOC appendix, spec §0, issue body, handoff — đồng nhất)
- [x] "7 ADR" — **PASS**
- [x] "32 term" — **FAIL** (35)
- [x] "19 user stories" — **PASS**
- [x] "21 OOS" — **PASS**
- [x] "64 Q&A" — **FAIL** (63 liệt kê, thiếu Q48)
- [x] "03sep26" cutoff — **PASS** (đồng nhất ở spec §1.3/§4.6/§5.1.1/§6#15?, code review, TOC; không xuất hiện trong CONTEXT.md — đúng, vì glossary không chứa ngày cutoff)
- [x] "2026-09-05" — **PASS**: `date -u` = Sat Sep 5 2026. Timeline khớp: issue #25 tạo 21:35:14Z → handoff timestamp 21:57 → commit `99c5fc8` 21:58:54Z → PR #26 merge 22:00:22Z

### 3.9 GitHub Issue #25
- [x] Tồn tại — **PASS** (là issue duy nhất của repo)
- [x] Title đúng từng chữ — **PASS**
- [x] State OPEN — **PASS**
- [x] Body link tới `docs/specs/v1-pd47ass.md` — **PASS** (4 link, đều resolve tại ref `arena/01a06dcc-pd-47-ass`; rủi ro F4)
- [x] Label `ready-for-agent` — **KHÁC HANDOFF**: đã apply ✓ (không cần nhắc chủ repo làm thủ công)
- [x] Không có comment — **PASS** (0 comment)
- [x] *Phát hiện thêm*: body issue lặp lại "Tests: **90/90 pass** ở 03sep26" (B1) và "64 câu Q&A, 16 round" (B4); body nói "Implementation Decisions (**17** mục con)" ✓ đúng (4.1–4.17), "Workflow (**6** bước)" ✓ đúng

---

## I. Không verify được (giới hạn)

1. **Trung thực của 64 câu paraphrase** — grilling transcript không persist (handoff §2 ghi rõ). Chỉ đối chiếu được Q-ref trong TOC/spec/ADR. Đây là lý do D2 và E7 chỉ dừng ở mức "nghi vấn".
2. **Chrome load test cho A1** — sandbox không có Chromium/Chrome binary (chỉ có node v22.22.3). Kết luận A1 dựa trên việc file `options.html` chắc chắn không tồn tại + manifest chắc chắn trỏ tới nó.
3. **Fixed point của code review có phải do chủ repo chỉ định hay agent tự chọn** — skill yêu cầu hỏi user nếu user không chỉ định; không có transcript để kiểm tra.

---

## J. Đề xuất thứ tự xử lý (chờ chỉ thị — chưa sửa gì)

**Nhóm 1 — quyết định kỹ thuật, cần chủ repo trả lời trước:**
1. A1: tạo `options.html` (dù chỉ placeholder) hay bỏ `options_page` khỏi manifest? Xác nhận trên máy thật xem extension có load không.
2. A2: chốt vị trí thật của `popup.html`/`popup.js`/`options.html` (root như manifest hiện tại, hay theo module map §2.2.1), rồi cập nhật **cả** §2.2.1 **và** §4.10.
3. A3: sửa dòng Story #4 trong code review thành ❌ Thiếu, và ghi rõ trong spec §4.3.1 rằng regex `#<videoId>` là **việc phải làm mới** (hiện không tồn tại trong `fetcher.js`).
4. D1: retry backoff — chốt "không làm" (sửa spec §7.1 #3 + §4.9.6 + OOS #17) hay hạ ADR 0003 xuống trạng thái `proposed`?
5. D2: Q54 hay Q23 cho "CS không cần source data"?

**Nhóm 2 — sửa số liệu (cơ học, ít rủi ro):** B1 (90→91, và bỏ gán cho `parser.test.mjs`), B2 (32→35), B3 (700→842), B4 (thêm Q48 hoặc sửa "64"→"63"), B5 (11 key→5 key), B6, B7 (nhãn fixed point), B8, B9, B10, B11, E4, E5 (`title`→`subTitle`), E6, G3.

**Nhóm 3 — glossary/format:** C1–C3 (dọn drift hoặc nới glossary), C4 (thêm `libass`, `MV3`, `PlayerAdapter`), C5 (dời implementation detail sang spec/ADR), C6 (thêm `_Avoid_` cho `FileEntry`), C7 (thêm term cho `ASSCEE_subIndex`, `ASSCEE_renderData`), D3 (quyết định giữ format 3 section hay theo template skill — nếu giữ, nên sửa mô tả trong handoff/checklist).

**Nhóm 4 — hygiene:** E1 (cân nhắc bỏ path/snippet khỏi Implementation Decisions, hoặc ghi rõ ngoại lệ), E3 (thêm `npm test`), E8 + F1 + F2 (bỏ câu "CONTEXT.md chưa được tạo / chưa có sẵn"), F3 (bổ sung module map), F4 (đổi link issue #25 sang `editor` hoặc SHA `99c5fc8`), F5 (sửa ngày trong AGENTS.md), G1/G2 (cập nhật handoff: đã commit + đã merge + label đã apply), G4 (sửa 3 lệnh shell hỏng).

---

## L. Đã sửa theo chỉ thị của chủ repo (05sep26, sau khi báo cáo này được viết)

Chủ repo trả lời 3 điểm nhóm 1 và chỉ thị: *"sửa hết nhóm 2, 3, 4; nhóm 1 nếu 3 ý trên đã trả lời thì sửa cả nhóm 1"*. Đã thực hiện — **không sửa code**, chỉ sửa tài liệu. `npm test` trước và sau đều **91/91 pass**.

**Trả lời của chủ repo làm căn cứ:**
1. `options.html` thiếu là **do chưa viết** (không phải manifest khai sai path).
2. Spec **nhầm** khi đưa `options.html` và `popup.js` vào thư mục con → giữ ở **repo root**.
3. `pipeline.txt` không nhắc `#<videoId>`; **auto-map là tính năng chưa hề có** (chủ repo hỏi lại "auto-map là gì?" → đã giải thích: khâu nối *video đang mở* → *file sub nào*, bằng cách tách `#<videoId>` khỏi **tên file**; khác hoàn toàn với `#` = prefix case-sensitive của search query trong `parseSearchQuery`).

**L1 — `docs/specs/v1-pd47ass.md` (33 chỗ):** header (64 câu → 16 round/63 câu truy vết được; bỏ "vì `CONTEXT.md` chưa được tạo"); §1.3 (path options về root, thêm cảnh báo manifest `options_page` trỏ file chưa có, test count 39/28/22/2 + toàn suite 91/91); §2.2.1 module map viết lại (popup/options ở root, thêm `background.test.mjs`, `package.json`, `CONTEXT.md`, `docs/adr/`, `v1-pd47ass.codereview.md`, bỏ `popup/` và `background/options.*`); §3.2 bỏ "(fansub author)"; §4.2.1 comment "text segment" → "text của mục base"; §4.2.2 + §4.9.1 `title` → `subTitle`; §4.3.1 thêm khối "Trạng thái 05sep26: CHƯA implement" + phân biệt `#` search token + gap "chưa chốt tách videoId lúc scan hay lúc lookup, `FileEntry` §4.2.3 chưa có field `videoId`"; §4.5.3 + §7.2 chú thích "segment" nghĩa khác; §4.10 thêm mục path manifest (giữ root) + "việc phải làm ngay: tạo `options.html`"; §4.14/§7.3 `user` → `viewer`; §4.15 path root; §5.1.1 test count từng file + toàn suite; §5.2 thêm `npm test`; §5.3 91/91; §6 OOS #4 chú thích Q25b; §8 thêm bước 0 (tạo `options.html`) + sửa path bước 4/5; Phụ lục A thêm Q20/Q22 + 3 hàng mới (§4.14, §4.15, §4.17); Phụ lục B bỏ "(fansub author)" + thêm note trỏ `CONTEXT.md` là glossary chính thức (40 term).

**L2 — `docs/specs/v1-pd47ass.toc.md` (11 chỗ):** §0 chú thích 63 câu/thiếu Q48; §1 bỏ "end-user", `11 key` → **5 key** (liệt kê đủ); story #13 bỏ "(fansub author)"; §4.1 path options/popup về root + `renderer.test.mjs` "low-level" → **mid-level**; §5.2 91/91; phụ lục header 63 câu; R11 chú thích "5 số câu nhưng 4 ý"; R12 chú thích `v1-renderer.md` là tên đề xuất lúc grilling, sau đổi thành `v1-pd47ass.md`, file đó chưa từng tồn tại.

**L3 — `docs/specs/v1-pd47ass.codereview.md` (23 chỗ):** header nói rõ fixed point = **baseline `ab3b264`** (99 files/8301) chứ không phải branch tip (111/10104), kèm phép tính 12 artifact = 1804 dòng; **thêm hard violation A.8** (manifest `options_page` trỏ file không tồn tại → extension không load; ghi rõ chủ repo xác nhận "chưa viết", và sandbox không có Chromium nên cần test lại trên máy thật); §A.1 2 → 3 dòng; §A.2 `popup/popup.js` → `popup.js` (root); §A.4 + hành động 3/4 path root; **thêm hành động 5b** (implement auto-map); §B.1 line 22 → 26; §B.9 line 99 → 97; §B.8 + phụ lục 366 → 365 dòng, `fetcher.js` 864 → 863; trục Spec Story #4 đổi từ "Cần verify code" → **❌ Thiếu** kèm bằng chứng grep và ghi chú đính chính; §D Standards "~5 judgement" → "~9 item trong 6/12 mùi", "7 hard" → "**8** hard"; §D Spec "~5/21" → "6/21 + 1 nửa", "~14 thiếu" → "14 (thêm auto-map)"; phụ lục test "90/90 theo pipeline" → "**đã chạy thật 91/91**".

**L4 — `CONTEXT.md` (viết lại theo `CONTEXT-FORMAT.md`):** 35 → **40 term** (thêm `libass`, `MV3`, `PlayerAdapter`, `sub index`, `renderer stat`); **40/40 term có `_Avoid_`** (thêm cho `FileEntry`); **dọn sạch implementation detail** ở 11 term (bỏ `ASSCEE_*` key, `crypto.randomUUID()`, `chrome.runtime.*`, field list của `FileEntry`/`parsedData`/`styleCss`/`lineCss`/`base`, đường dẫn `background/parser.js`, ngưỡng "30% trong 5 giây", biến `nodesTouched`, quyết định "CS không fetch/parse" — những thứ đó thuộc spec §4.2/§4.9 và ADR 0001/0005/0006); `base._Avoid_` thêm phân biệt "segment" của pretext/`\move`; header nói rõ file không chứa storage key/tên hàm/đường dẫn/ngưỡng số. Verify sau sửa: `grep -c '^\*\*'` = 40, `grep -c '_Avoid_'` = 40, `grep "ASSCEE_|crypto\.|chrome\.|\.js|nodesTouched|30%|randomUUID|sendMessage"` = **0**.

**L5 — `AGENTS.md` + `docs/agents/domain.md`:** bỏ 2 câu stale "(`CONTEXT.md` + `docs/adr/`) … chưa có sẵn" và "until `CONTEXT.md` exists" → thay bằng trạng thái thật (40 term, 7 ADR, từ 05sep26); sửa ngày `3cca18b` (commit upstream đề 04sep26, cài vào repo 04sep26, không phải 05sep26).

**L6 — `.scratch/handoff-2026-09-05-2157.md`:** thêm banner ĐÍNH CHÍNH ở đầu (giải thích 3 mệnh đề lỗi thời là do chủ repo merge + sửa label *sau khi* file được viết); **tick 52 checkbox** `- [ ]` → `- [x]`; sửa 18 chỗ (số dòng spec 700 → 842, 32 term → 35/40, 90/90 → 91/91 trong cây thư mục §7, 3 mệnh đề §8 gạch ngang + ghi chú, §3.9 label đã apply, §3.8 64 Q&A không verify được); **sửa 3 lệnh shell hỏng** ở §6 (cmd5 đếm OOS bằng `awk` + `grep -cE`, cmd8 đổi sang grep từ khóa quyết định thay vì slug, cmd6 thêm cảnh báo 5 term gạch nối gây false-negative).

**L7 — Issue #25: KHÔNG sửa được.** `gh issue edit 25` và `gh issue comment 25` đều trả **"Resource not accessible by integration"** → bot token Arena không có quyền ghi issue trong repo này (cùng lớp lỗi khiến session trước không tự apply được label; lần đó chủ repo làm tay). Đã soạn sẵn 2 file để chủ repo tự chạy bằng token của mình:
- `.scratch/issue25-body-dinhchinh-05sep26.md` — body mới (4 link đổi từ ref `arena/01a06dcc-pd-47-ass` sang **`editor`**; "90/90" → "91/91"; "64 câu" → "16 round/63 câu truy vết được"; mục "Kiến trúc hiện tại" thêm `CONTEXT.md` + `docs/adr/`; thêm section "Đính chính 05sep26"). Lệnh: `gh issue edit 25 --body-file .scratch/issue25-body-dinhchinh-05sep26.md`
- `.scratch/issue25-comment-ready-to-paste.md` — bản ngắn nếu anh chỉ muốn comment thay vì sửa body.

**L8 — ✅ ĐÃ sửa nốt (chủ repo trả lời lúc ~23:10 UTC 05sep26), không còn điểm nào treo:**
- **D1** retry exponential backoff → chủ repo chốt **KHÔNG làm**. Sửa 6 chỗ: ADR 0003 (`Status: accepted` + ngày xác nhận), spec §4.9.6, spec OOS #17, spec §7.1 (rút mục #3 → còn 6, đánh số lại 1–6 + ghi chú), spec §8 ("Chốt 7" → "Chốt 6"), TOC §4.9 + TOC §7 mục 3 (chú thích đã chốt). Hai file nháp issue trong `.scratch/` cũng cập nhật theo.
- **D2** → chủ repo xác nhận mệnh đề đúng và cho lý do (**content-side** vs background-side). Sửa 4 chỗ: ADR 0001 (ghi lý do + chú thích vì sao bỏ trích Q54), spec §4.5.1 (như vậy), `CONTEXT.md` term `content script` (sharpen thành content-side/background-side), TOC §4.7 (chú thích đính chính). Số Q không kiểm chứng được nên **không** thay bằng Q23 — thay bằng lý do tự thân, bền hơn.

**L9 — Không đụng tới:** `pipeline.txt` (ghi "90 pass (03sep26)" là **đúng lịch sử** tại ngày đó — không sửa), `to-do.txt`, `manifest.json`, `popup.html`, toàn bộ `background/`, `tests/`, `docs/adr/*` (7 ADR giữ nguyên văn — chỉ có D1/D2 cần quyết mới sửa được), `.agents/skills/` (skill upstream, không tự ý sửa).

## K. Lệnh đã dùng (để tái lập)

```bash
git status; git log --oneline -1                      # sạch, HEAD = 7ad2074
gh api repos/PhongChillFansub/PD-47.ass/git/trees/ab3b264?recursive=1   # 99 blob
gh api repos/PhongChillFansub/PD-47.ass/git/trees/99c5fc8?recursive=1   # 111 blob
gh api repos/PhongChillFansub/PD-47.ass/compare/ab3b264...99c5fc8       # 12 addition, 0 modify
gh pr view 26 --json baseRefName,headRefName,state,mergedAt              # base = editor, MERGED
gh issue view 25 --json title,state,labels,body; gh api .../issues/25/comments   # OPEN, label đã có, 0 comment
npm test                                                                 # 91 pass / 0 fail
for f in tests/*.test.mjs; do node --test "$f"; done                     # 2 / 39 / 28 / 22
grep -c '^\*\*' CONTEXT.md                                               # 35
grep -n "videoId" background/fetcher.js                                  # 0 kết quả
find . -name "options*" -not -path "./.git/*"                            # 0 kết quả
grep -o '"(service_worker|options_page|default_popup)"…' manifest.json   # options.html MISS
```
