# Handoff — Verify lại output của session `arena/01a06dcc-pd-47-ass`

> **Mục đích**: Session này (agent mới) sẽ **kiểm tra lại** toàn bộ output của session trước. Chủ repo nghi ngờ có thể xuất hiện sai sót do context quá lớn (~8300 dòng diff + 16 round grilling + 7 ADR + spec 700 dòng).
>
> **CẢNH BÁO**: Skill `/handoff` khuyến nghị lưu ở temp directory (vd `/tmp/`), nhưng chủ repo yêu cầu lưu trong workspace (`/home/user/PD-47.ass/.scratch/`) với timestamp. File này có thể bị ignore bởi `.gitignore` (kiểm tra trước khi commit).
>
> **Branch**: `arena/01a06dcc-pd-47-ass` (chưa push — session này chưa commit/push bất cứ thay đổi nào).

---

## 1. Tóm tắt phiên trước

Phiên trước đã làm 4 việc chính:

1. **Grilling 64 câu Q&A qua 16 round** → sinh outline spec.
2. **Viết spec** `docs/specs/v1-pd47ass.md` (~700 dòng) + phụ lục TOC.
3. **Publish GitHub Issue #25** + tạo label `ready-for-agent` (apply label fail do bot token permission).
4. **Chạy `/code-review`** 2 trục (Standards + Spec) → báo cáo `docs/specs/v1-pd47ass.codereview.md`.
5. **Chạy `/domain-modeling`** → `CONTEXT.md` (32 term) + 7 ADR trong `docs/adr/`.

**Rủi ro sai sót** (theo chủ repo):
- Context quá lớn có thể khiến agent "hallucinate" hoặc mâu thuẫn giữa các file output.
- Grilling dài 64 câu có thể có hiểu sai ý chủ repo ở 1-2 chỗ.
- Spec 700 dòng copy/paste từ grilling có thể có chỗ paraphrase sai.
- 7 ADR viết liên tục có thể có chỗ nội dung trùng hoặc mâu thuẫn.

---

## 2. Artifacts đã tạo (KHÔNG trùng nội dung — tham chiếu path)

| Artifact | Path | Vài |
|---|---|---|
| Outline spec | `docs/specs/v1-pd47ass.toc.md` | 16 round Q&A tóm tắt |
| Spec chính thức | `docs/specs/v1-pd47ass.md` | 8 phần, 700 dòng |
| Code review report | `docs/specs/v1-pd47ass.codereview.md` | 2 trục + tổng kết |
| Domain context | `CONTEXT.md` | 32 term glossary |
| ADR #1 | `docs/adr/0001-bg-single-source-of-truth.md` | BG = single source of truth |
| ADR #2 | `docs/adr/0002-chuan-bo-sung.md` | Chuẩn bổ sung |
| ADR #3 | `docs/adr/0003-per-source-cooldown-60s.md` | Cooldown 60s |
| ADR #4 | `docs/adr/0004-dom-css-with-canvas-fallback.md` | Hybrid render |
| ADR #5 | `docs/adr/0005-per-video-slot-auto-overwrite.md` | Slot auto-overwrite |
| ADR #6 | `docs/adr/0006-skip-frame-dynamic-plus-auto-detect.md` | Skip frame dynamic+detect |
| ADR #7 | `docs/adr/0007-stroke-x2-for-borderstyle-1.md` | Stroke ×2 |
| Handoff (file này) | `.scratch/handoff-2026-09-05-2157.md` | Verify checklist |
| GitHub Issue | https://github.com/PhongChillFansub/PD-47.ass/issues/25 | Spec published |
| Grilling transcript | (chat history — không persist) | 16 round Q&A |

**Repo không thay đổi code** — chỉ thêm file mới. Code hiện tại vẫn ở `ab3b264` (cutoff 03sep26).

---

## 3. CHECKLIST verify (agent mới làm tuần tự)

> Mỗi mục có hướng dẫn cụ thể. Tick `[x]` khi verify xong. Nếu phát hiện sai, **KHÔNG TỰ SỬA** — báo cáo chủ repo, đợi chỉ thị.

### 3.1 Cross-reference giữa các artifact (quan trọng nhất)

Vì session trước viết nhiều file dài, kiểm tra tính nhất quán:

- [ ] **CONTEXT.md vs code**: 32 term trong glossary có xuất hiện đúng trong code không? Đặc biệt các term code-internal: `FALLBACK_DEFAULT_STYLE`, `subData`, `subIndex`, `renderData`. Grep:
  ```bash
  grep -rn "FALLBACK_DEFAULT_STYLE\|subData\|subIndex\|renderData" background/ | head -20
  ```
- [ ] **CONTEXT.md vs spec**: spec dùng đúng term glossary (không dùng "end-user" khi glossary chốt "viewer", không dùng "segment" khi glossary chốt "base").
  ```bash
  grep -n "end-user\|user\b\|fansub author\|segment" docs/specs/v1-pd47ass.md
  ```
- [ ] **7 ADR có mâu thuẫn với nhau không?** Ví dụ: ADR #1 nói "CS thuần renderer" mà ADR #4 nói "CS dùng DOM+canvas" → OK vì canvas vẫn là renderer. Nhưng nếu ADR nào nói "CS fetch" là sai.
- [ ] **Spec vs 7 ADR**: spec có vi phạm ADR nào không? Ví dụ spec nói "cooldown 60s" (đúng ADR #3), spec nói "DOM+canvas" (đúng ADR #4).
- [ ] **Code review vs spec**: code review có chỉ ra finding mà spec đã cover chưa?

### 3.2 Verify spec `v1-pd47ass.md`

- [ ] **8 phần đầy đủ**: Problem Statement, Solution, User Stories, Implementation Decisions, Testing Decisions, Out of Scope, Further Notes, Workflow tiếp theo. Theo template `/to-spec` (xem `.agents/skills/to-spec/SKILL.md`).
- [ ] **19 user stories** đánh số đúng, không trùng, không thiếu.
- [ ] **21 OOS** đánh số đúng, không trùng, không thiếu.
- [ ] **Open questions §7.1** có 7 mục (đã chốt trong grilling).
- [ ] **Số liệu nhất quán**: 6 module background + 4 test + 1 HTML + 1 manifest + 1 package.json. Đếm lại trong `docs/specs/v1-pd47ass.md §2.2.1`.
- [ ] **Phụ lục A "Tham chiếu chéo"** trỏ đúng §.
- [ ] **Phụ lục B "Glossary"** — 11 term (Aegisub, ASS, libass, MV3, rVFC, Karaoke, pretext, chuẩn bổ sung, PlayerAdapter, sub provider, sub slot). Có thể trùng với `CONTEXT.md` (OK) nhưng phải cùng định nghĩa.

### 3.3 Verify TOC `v1-pd47ass.toc.md`

- [ ] **Tên file trong workflow §8** = `docs/specs/v1-pd47ass.md` (không phải `v1-renderer.md`).
- [ ] **Phần §4.3** tham chiếu `docs/specs/v1-pd47ass` (không phải `v1-renderer`).
- [ ] **§6 Out of Scope** có ghi chú giải thích Q33 (chủ repo nhắc popup.js riêng, không từ chối OOS).
- [ ] **16 round × ~4 câu/round = ~64 câu** trong phụ lục.
- [ ] **TOC đã được chốt "archive as appendix"** trong grilling (không xóa).

### 3.4 Verify code review `v1-pd47ass.codereview.md`

- [ ] **2 trục rõ ràng**: Standards + Spec, không merge lẫn.
- [ ] **Hard violations §A có 7 mục**: background.js rỗng, popup.js rỗng, content script chưa có, options page chưa có, classify 2.3 + 2.1 stub, addSubData TODO, manifest host_permissions.
- [ ] **Smell baseline §B có 12 mục** (Mysterious Name, Duplicated Code, Feature Envy, Data Clumps, Primitive Obsession, Repeated Switches, Shotgun Surgery, Divergent Change, Speculative Generality, Message Chains, Middle Man, Refused Bequest).
- [ ] **Tổng kết §D** đúng: 7 hard + ~5 judgement (Standards), ~14/21 thiếu (Spec).
- [ ] **Worst issue mỗi trục**: Standards = background.js rỗng, Spec = BG message bus thiếu. Có khớp với §A.1 + §D không?

### 3.5 Verify `CONTEXT.md`

- [ ] **Header**: "PD-47.ass — Domain Context" + 2 câu mô tả.
- [ ] **4 nhóm** đầy đủ: Actors (3) + ASS/Aegisub (8) + Architecture (11) + Renderer (10) = 32 term.
- [ ] **Mỗi term có _Avoid_** (theo format yêu cầu).
- [ ] **Không chứa implementation detail** (đúng rule của skill).
- [ ] **Không chứa general programming concept** (vd "Promise", "regex", "queue" — không có trong glossary, OK vì đó là general concept).

### 3.6 Verify 7 ADR

Mỗi ADR phải đúng format: tiêu đề ngắn + 1-3 câu tóm tắt. Đủ 3 tiêu chí (hard to reverse + surprising + real trade-off).

- [ ] **0001 BG single source of truth**: 3-4 câu, có "tại sao" ở cuối.
- [ ] **0002 chuẩn bổ sung**: có 2 phần (filename + 8 keys) + lý do giới hạn 8 keys.
- [ ] **0003 cooldown 60s**: có so sánh với retry backoff.
- [ ] **0004 DOM+canvas**: có so sánh 3 hướng (100% DOM, 100% canvas, hybrid).
- [ ] **0005 slot auto-overwrite**: có so sánh với ETag/Update Details.
- [ ] **0006 skip frame dynamic+detect**: có 2 cơ chế (skip dynamic + auto-detect) + threshold 30% trong 5s.
- [ ] **0007 stroke ×2**: có giải thích "magic number 2" (cân giữa vs vẽ ngoài).

### 3.7 Verify không có sensitive info

- [ ] Không có API key, password, GitHub token.
- [ ] Không có email cá nhân, PII.
- [ ] GitHub URL dùng org/repo public (`PhongChillFansub/PD-47.ass`).
- [ ] Issue #25 là public.

### 3.8 Verify nội bộ nhất quán

- [ ] **Số liệu "16 round grilling"** xuất hiện đồng nhất ở: TOC phụ lục, spec §0 "Nguồn spec hội thoại", handoff này.
- [ ] **Số liệu "7 ADR"** đúng (đếm file trong `docs/adr/`).
- [ ] **Số liệu "32 term"** trong CONTEXT.md (đếm `**X**:`).
- [ ] **Số liệu "19 user stories"** trong spec §3.
- [ ] **Số liệu "21 OOS"** trong spec §6.
- [ ] **Số liệu "64 Q&A"** trong TOC + spec + handoff.
- [ ] **Date "03sep26"** (cutoff code) xuất hiện đồng nhất ở: spec, code review, CONTEXT.md (nếu có).
- [ ] **Date "2026-09-05"** (session date) — verify bằng `date` command trong shell.

### 3.9 Verify GitHub Issue

- [ ] **Issue #25 tồn tại**: https://github.com/PhongChillFansub/PD-47.ass/issues/25
- [ ] **Title**: "Spec v1-pd47ass: Hoàn thiện extension v0.1.0 chạy end-to-end trên YouTube"
- [ ] **State**: OPEN.
- [ ] **Body có link** tới `docs/specs/v1-pd47ass.md` trong repo.
- [ ] **Label `ready-for-agent`**: CHƯA APPLY (do bot permission). Nhắc chủ repo apply thủ công.
- [ ] **Không có comment** (bot không có quyền comment, có comment cũng lỗi).

---

## 4. Câu hỏi chủ repo có thể hỏi session sau

1. **"Spec có thực sự đúng ý tôi không?"** → Mở TOC + spec, đối chiếu với 16 round grilling. Nếu chỗ nào paraphrase sai → flag.
2. **"CONTEXT.md có chỗ nào mâu thuẫn code không?"** → Grep 32 term trong code, kiểm tra định nghĩa.
3. **"7 ADR có cái nào thừa không?"** → Check 3 tiêu chí (hard to reverse + surprising + real trade-off). Nếu ADR nào không đủ → flag để xóa.
4. **"Code review có chỗ nào sai không?"** → Mở code thật, đối chiếu findings. Ví dụ §A.6 nói "addSubData còn TODO" — kiểm tra `background/storage.js` có còn comment đó không.
5. **"Có output nào chưa commit không?"** → `git status` để xem. Lưu ý: session trước KHÔNG commit, nên tất cả file mới đều untracked.

---

## 5. Suggested skills cho session sau

Theo skill `/handoff`, đây là các skill session sau nên gọi:

1. **`/grilling`** — Nếu cần đào sâu 1 quyết định đáng ngờ (vd 1 ADR có vẻ sai).
2. **`/code-review`** — Có thể chạy lại với fixed point khác (vd review diff của 1 commit sau này).
3. **`/diagnosing-bugs`** — Nếu phát hiện sai sót trong output, dùng để tìm nguyên nhân.
4. **`/teach`** — Nếu chủ repo muốn giải thích 1 khái niệm (vd pretext, rVFC, MV3).

**Không nên dùng** trong session verify:
- `/implement` — chưa cần implement, chỉ verify.
- `/domain-modeling` — CONTEXT.md đã có, không thay đổi trừ khi phát hiện term sai.
- `/to-spec` — spec đã có, chỉ sửa nếu sai.

---

## 6. Lệnh shell hữu ích cho session verify

```bash
# 1. Xem trạng thái git
git status
git log --oneline -5

# 2. Đếm số ADR
ls docs/adr/*.md | wc -l

# 3. Đếm term trong CONTEXT.md
grep -c "^\*\*[^*]" CONTEXT.md

# 4. Đếm user stories trong spec
grep -c "^[0-9]*\. As a" docs/specs/v1-pd47ass.md

# 5. Đếm OOS trong spec
grep -c "^- \*\*[^*]" docs/specs/v1-pd47ass.md

# 6. Grep 32 term glossary trong code
for term in viewer sub-provider maintainer Aegisub ASS karaoke clip \
            background content-script popup options-page source \
            FileEntry sub-slot videoId storageId chuẩn-bổ-sung \
            rVFC pretext parsedData styleCss lineCss base \
            FALLBACK_DEFAULT_STYLE skip-frame FPS NPS DFPS sub-DOM; do
  echo "=== $term ==="
  grep -rln "$term" background/ docs/specs/ CONTEXT.md 2>/dev/null | head -3
done

# 7. Verify GitHub Issue
gh issue view 25 --json title,state,labels,body

# 8. So sánh ADR với spec (cùng decision không?)
for adr in docs/adr/*.md; do
  echo "=== $adr ==="
  grep -l "$(basename $adr .md | sed 's/^[0-9]*-//')" docs/specs/v1-pd47ass.md 2>/dev/null
done
```

---

## 7. Cấu trúc file & đường dẫn quan trọng

```
/home/user/PD-47.ass/                  # repo root, branch arena/01a06dcc-pd-47-ass
├── CONTEXT.md                         # NEW: glossary (32 term)
├── AGENTS.md                          # CŨ: quy ước agent
├── pipeline.txt                       # CŨ: architecture doc
├── manifest.json                      # CŨ: MV3 manifest
├── background/                        # CŨ: BG source
│   ├── background.js                  # CŨ: alpha, rỗng
│   ├── fetcher.js                     # CŨ: beta
│   ├── parser.js                      # CŨ: beta
│   ├── storage.js                     # CŨ: alpha
│   ├── tagProcess.js                  # CŨ: partial
│   └── utils.js                       # CŨ
├── popup.html                         # CŨ: 4 cột
├── popup.js                           # CŨ: rỗng
├── tests/                             # CŨ: 90/90 pass
├── docs/
│   ├── agents/                        # CŨ: meta cho agent
│   ├── adr/                           # NEW: 7 ADR
│   │   ├── 0001-bg-single-source-of-truth.md
│   │   ├── 0002-chuan-bo-sung.md
│   │   ├── 0003-per-source-cooldown-60s.md
│   │   ├── 0004-dom-css-with-canvas-fallback.md
│   │   ├── 0005-per-video-slot-auto-overwrite.md
│   │   ├── 0006-skip-frame-dynamic-plus-auto-detect.md
│   │   └── 0007-stroke-x2-for-borderstyle-1.md
│   └── specs/                         # NEW
│       ├── v1-pd47ass.md              # spec chính thức (~700 dòng)
│       ├── v1-pd47ass.toc.md          # TOC (phụ lục lịch sử)
│       └── v1-pd47ass.codereview.md   # code review report
├── .scratch/
│   └── handoff-2026-09-05-2157.md     # NEW: file này
└── .agents/skills/                    # CŨ: 25 skill từ mattpocock
```

---

## 8. Note quan trọng

- **Tất cả file mới đều UNTRACKED** (chưa commit). Cần `git add` + `git commit` thủ công nếu muốn lưu lại.
- **Issue #25 chưa apply label `ready-for-agent`** do bot token permission. Có thể chủ repo apply thủ công.
- **Session trước KHÔNG push branch** lên origin. Tất cả thay đổi chỉ local.
- **Context của session trước** rất lớn (~8300 dòng diff + 16 round Q&A). Khi verify, **đọc artifacts trước, chat history sau** — artifacts đã được distillate sẵn.
- **Skill `/handoff` khuyến nghị lưu ở `/tmp/`** nhưng chủ repo yêu cầu trong workspace. Có thể move file này ra `/tmp/` sau khi verify nếu thấy không cần persist.
